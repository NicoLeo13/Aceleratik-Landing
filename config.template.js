// Configuración de API keys y variables sensibles - TEMPLATE
// Renombrar este archivo a config.js y reemplazar los valores con las claves reales
const CONFIG = {
  // Clave API de Google Maps para el mapa de contacto
  GOOGLE_MAPS_API_KEY: "YOUR_GOOGLE_MAPS_API_KEY",
};

window.CONFIG = CONFIG;
